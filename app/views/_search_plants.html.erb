

<%= form_with(url: plants_path, method: 'get', local: true) do %>
           <%= label_tag :name %>
           <%= text_field_tag :name, nil, placeholder: 'Search for a a plant' %>
            <%= button_tag 'Search' %>
       <% end %>

